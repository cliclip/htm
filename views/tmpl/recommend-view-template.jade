script(type="text/template",id="recommend-view-template")
  div.masker_layer
  section.small_pop
    span.close_w
    h2.subject_forward 转发
    ul.pop_body
      li.select_user.clearfix
        div.container
          div.user_small_head#user_image
            img(id="imgId")
          input(type="text",id="recomm_name",name="name")
      div.name_list#name_listDiv
      li.input_textarea
        |<textarea id="recommend_text">说点啥吧～</textarea>
      <% if(!App.util.self(clipid.split(":")[0])) {%>
      li.input_checkbox
        label
          input(type="checkbox",id="reclip_box")
          | 同时收录
      <%}%>
      li.btn_group.clearfix
        input.verify(type="button",value="确定",id="submit")
        input.cancel(type="button",value="取消",id="cancel")

script(type="text/template",id="namelist-view-template")
  | <span class="user_list_head"><img src="<%=App.util.face_url(face)%>"></img></span>&nbsp;&nbsp;
  | <span class="name-li" id="<%=name%>_<%=id%>" title="<%=face%>"><%=name%></span>
