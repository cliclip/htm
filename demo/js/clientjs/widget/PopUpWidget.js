PopUpWidget = function(_container,_options){	this.container = _container;	this.options = _options;	var popupStatus = 0;	this.view = Backbone.View.extend({		el:$(container),		initialize:function(){			//this.render();		},		render:function(){			centerPopup();  			loadPopup();		},		events:{			"click #popupContactClose":"disablePopup",			"click #backgroundPopup":"disablePopup"		}		loadPopup:function(){  			//loads popup only if it is disabled  			if(popupStatus==0){  				$("#backgroundPopup").css({  					"opacity": "0.7"  				});  				$("#backgroundPopup").fadeIn("slow");  				$("#popupContact").fadeIn("slow");  				popupStatus = 1;  			}  		},		disablePopup:function(){  			//disables popup only if it is enabled  			if(popupStatus==1){  				$("#backgroundPopup").fadeOut("slow");  				$("#popupContact").fadeOut("slow");  				popupStatus = 0;  			}  		},		centerPopup:function(){  			//request data for centering  			var windowWidth = _options.clientWidth;  			var windowHeight = _options.clientHeight;  			var popupHeight = _options.height;  			var popupWidth = _options.width;  			//centering  			$("#popupContact").css({  				"position": "absolute",  				"top": windowHeight/2-popupHeight/2,  				"left": windowWidth/2-popupWidth/2  			});  			//only need force for IE6  			  			$("#backgroundPopup").css({  				"height": windowHeight  			});  		  		},			})}PopUpWidget.prototype.render = function(){	this.view.render();}PopUpWidget.prototype.loadWidget(widget){	this.render();	widget.render();}